import React from "react";
import StorageItem from "../StorageItem";

export default function StorageItemList({selectedItem, productList}) {
  let storageList;
  function handleSelectedItemsList(selectedItem) {

  }
  return (    
  <div>
    <div className="listedItems">

    </div>
  </div>
  )
}